name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      environment:
        description: "Ambiente de deploy"
        required: true
        type: choice
        options:
          - test
          - production

permissions:
  contents: read
  packages: write
  id-token: write

concurrency:
  group: deploy-${{ github.event.inputs.environment || 'test' }}
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}/ln-educacional

jobs:
  resolve-env:
    name: Resolve Environment
    runs-on: ubuntu-latest
    outputs:
      environment: ${{ steps.env.outputs.environment }}
      vite_api_url: ${{ steps.env.outputs.vite_api_url }}
      deploy_path: ${{ steps.env.outputs.deploy_path }}
      env_tag: ${{ steps.env.outputs.env_tag }}
    steps:
      - id: env
        run: |
          set -euo pipefail
          ENV="${{ github.event.inputs.environment || 'test' }}"
          echo "environment=$ENV" >> "$GITHUB_OUTPUT"

          if [ "$ENV" = "production" ]; then
            echo "vite_api_url=/api" >> "$GITHUB_OUTPUT"
            echo "deploy_path=/opt/apps/ln-educacional/prod" >> "$GITHUB_OUTPUT"
            echo "env_tag=prod" >> "$GITHUB_OUTPUT"
          else
            echo "vite_api_url=/api" >> "$GITHUB_OUTPUT"
            echo "deploy_path=/opt/apps/ln-educacional/test" >> "$GITHUB_OUTPUT"
            echo "env_tag=test" >> "$GITHUB_OUTPUT"
          fi

  build-and-push:
    name: Build & Push Images
    runs-on: ubuntu-latest
    needs: resolve-env
    environment: ${{ needs.resolve-env.outputs.environment }}
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push server
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.server
          push: true
          tags: |
            ${{ env.IMAGE_PREFIX }}-server:${{ github.sha }}
            ${{ env.IMAGE_PREFIX }}-server:${{ needs.resolve-env.outputs.env_tag }}
            ${{ env.IMAGE_PREFIX }}-server:latest
          cache-from: type=gha,scope=ln-server
          cache-to: type=gha,mode=max,scope=ln-server

      - name: Build & push client
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.client
          push: true
          tags: |
            ${{ env.IMAGE_PREFIX }}-client:${{ github.sha }}
            ${{ env.IMAGE_PREFIX }}-client:${{ needs.resolve-env.outputs.env_tag }}
            ${{ env.IMAGE_PREFIX }}-client:latest
          build-args: |
            VITE_API_URL=${{ needs.resolve-env.outputs.vite_api_url }}
          cache-from: type=gha,scope=ln-client
          cache-to: type=gha,mode=max,scope=ln-client

      - name: Trivy scan - server
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: ${{ env.IMAGE_PREFIX }}-server:${{ github.sha }}
          format: table
          exit-code: 1
          severity: CRITICAL,HIGH
          ignore-unfixed: true

      - name: Trivy scan - client
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: ${{ env.IMAGE_PREFIX }}-client:${{ github.sha }}
          format: table
          exit-code: 1
          severity: CRITICAL,HIGH
          ignore-unfixed: true

      - name: Install Cosign
        uses: sigstore/cosign-installer@v3

      - name: Sign server image
        run: |
          cosign sign --yes \
            ${{ env.IMAGE_PREFIX }}-server:${{ github.sha }}

      - name: Sign client image
        run: |
          cosign sign --yes \
            ${{ env.IMAGE_PREFIX }}-client:${{ github.sha }}

  deploy:
    name: Deploy to ${{ needs.resolve-env.outputs.environment }}
    runs-on: ubuntu-latest
    needs: [resolve-env, build-and-push]
    environment: ${{ needs.resolve-env.outputs.environment }}
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          port: ${{ secrets.VPS_SSH_PORT }}
          username: ${{ secrets.VPS_SSH_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -euo pipefail

            DEPLOY_PATH="${{ needs.resolve-env.outputs.deploy_path }}"
            IMAGE_TAG="${{ github.sha }}"

            cd "$DEPLOY_PATH"

            # Salvar tag anterior para rollback
            OLD_TAG=$(grep '^IMAGE_TAG=' .env 2>/dev/null | cut -d= -f2 || echo "")

            # Atualizar tag da imagem
            sed -i "s|^IMAGE_TAG=.*|IMAGE_TAG=$IMAGE_TAG|" .env || echo "IMAGE_TAG=$IMAGE_TAG" >> .env

            # Login no GHCR (necessario para repos privados)
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            # Pull e deploy
            docker compose pull
            docker compose up -d --remove-orphans

            # Health check (aguardar server responder)
            echo "Aguardando health check..."
            HEALTHY=false
            for i in $(seq 1 30); do
              if docker compose exec -T server sh -c "wget -qO- http://localhost:3333/health" 2>/dev/null; then
                echo ""
                echo "Deploy concluido com sucesso!"
                HEALTHY=true
                break
              fi
              sleep 2
            done

            # Rollback se health check falhou
            if [ "$HEALTHY" = "false" ]; then
              echo "Health check falhou apos 30 tentativas"
              docker compose logs --tail=50

              if [ -n "$OLD_TAG" ]; then
                echo "Revertendo para tag anterior: $OLD_TAG"
                sed -i "s|^IMAGE_TAG=.*|IMAGE_TAG=$OLD_TAG|" .env
                docker compose pull
                docker compose up -d --remove-orphans
                echo "Rollback concluido"
              fi

              exit 1
            fi

            # Limpar imagens antigas
            docker image prune -f
